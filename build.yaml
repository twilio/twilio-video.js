name: signal-js
targets:
  - cent6
build:
  dependencies:
    - node
  commands:
    - make clean
    - make
    - rm -rf sdk
    - mv build/sdk .
contents:
  include:
    - sdk
install:
  prefix: /mnt
