version: 2
jobs:
  build:
    machine: true
    environment:
      BROWSER: chrome
      TOPOLOGY: group
      ENVIRONMENT: prod
      BVER: stable
    steps:
      - run: echo building $CIRCLE_BRANCH !
      - checkout
      - run: scripts/circleci-run-tests.sh
      # Save artifacts
      - store_artifacts:
          path: ./logs
          destination: build
